{
  "framework": "vite",
  "buildCommand": "vite build",
  "outputDirectory": "dist-demo",
  "devCommand": "vite dev --port $PORT",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
